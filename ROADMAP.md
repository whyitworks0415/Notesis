# Notesis — 설계 문서

안드로이드 태블릿 필기 앱. 로컬 파일 저장, WebView AI 사이드바, 문서 임포트.

## 0. 대전제

> **필기 엔진을 직접 만들지 않는다.**

`androidx.ink` 1.0.0 (2025-12-17 stable)은 구글이 Keep/Docs에서 쓰는 C++ 잉크 엔진이다.
스트로크 지오메트리, 브러시, 모션 예측, 프론트버퍼 저지연 렌더링이 전부 들어있다.
직접 구현하면 6개월 걸리고 결과는 더 나쁘다. 여기가 이 프로젝트의 유일한 진짜 레버다.

## 1. 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 언어/플랫폼 | Kotlin, 네이티브 Android | 스타일러스 저지연 API가 네이티브 전용. Flutter/RN은 여기서 진다 |
| 잉크 | `androidx.ink:ink-{authoring,brush,geometry,rendering,strokes}:1.0.0` | 위 참조 |
| 저지연 | `InProgressStrokesView` (ink-authoring 내장) | 프론트버퍼 + 예측을 알아서 함. `graphics-core`를 직접 만질 일 없음 |
| UI 크롬 | Jetpack Compose (Material 3 Expressive) | 툴바/서랍/노트목록. 캔버스만 View로 interop |
| 저장 | 로컬 파일 + Room(SQLite) 인덱스 | 서버 없음. 파일이 원본, DB는 검색용 캐시 |
| 필기→텍스트 | ML Kit Digital Ink Recognition (온디바이스, 한국어 지원) | 검색과 AI 전달용. 모델 ~20MB, 다운로드형 |
| AI | WebView 사이드 패널 | 아래 §5 |
| PDF | `androidx.pdf` / `PdfRenderer` | OS 내장. PDF 라이브러리 추가 금지 |

**넣지 않는 것:** 로컬 LLM, 동기화 서버, 계정 시스템, 커스텀 렌더 엔진, DI 프레임워크(Hilt조차 초기엔 불필요), 멀티모듈 분리.
"무겁지 않게"는 기능을 빼는 게 아니라 **의존성과 레이어를 안 쌓는 것**으로 달성한다.

## 2. 성능 — 측정 가능한 목표

숫자 없는 "빠르게"는 의미 없다. P0에서 이걸 못 맞추면 설계를 되돌린다.

- 모션-투-포톤: **120Hz 기기에서 예측 켜고 ≤ 25ms**
- 스트로크 10,000개 노트에서 팬/줌 시 **드롭 프레임 0**
- 앱 콜드 스타트 → 필기 가능까지 **≤ 800ms**
- APK **≤ 30MB** (ML Kit 모델 제외)

측정: `adb shell dumpsys gfxinfo <pkg> framestats`, JankStats, 그리고 240fps 슬로모 카메라로 펜촉-잉크 간격 실측.
**에뮬레이터로는 지연을 측정할 수 없다. 실기기 필수.**

### 렌더링 구조 (여기서 대부분 망함)

- **젖은 잉크**(그리는 중): `InProgressStrokesView`가 프론트버퍼에 직접 그림. 손 안 댐.
- **마른 잉크**(확정): 별도 `SurfaceView`에 `CanvasStrokeRenderer`로.
  - 뷰포트 컬링 필수 — `PartitionedMesh` 바운딩박스로 화면 밖 스트로크 스킵.
  - 스트로크가 많아지면 타일 캐시(비트맵 타일 512px)로 승격.
  - `// ponytail:` 초기엔 컬링만. 실측에서 프레임 떨어질 때 타일 캐시 추가.

## 3. 저장 포맷

노트 하나 = 디렉터리 하나. ZIP 아님(부분 저장 불가해서 느려짐).

```
Notesis/notes/<uuid>/
  meta.json          제목, 생성/수정시각, 태그, 페이지 순서
  pages/<n>.strokes  스트로크 바이너리 (아래)
  assets/            임포트한 PDF 원본, 이미지
  text.txt           OCR 결과 (검색용, 없어도 됨)
```

`.strokes`는 **`androidx.ink:ink-storage`**로 인코딩한다. 자체 포맷을 짜려던 계획은 폐기 —
이 모듈이 `StrokeInputBatch`와 `BrushFamily`를 protobuf + gzip으로 encode/decode 해주고,
저장하는 것이 렌더링 결과가 아니라 **원시 입력**이라서 라이브러리 버전이 올라가도 파일이 안 깨진다.
용량도 가장 작다. (`StrokeInputBatchSerialization.encode(batch, outputStream)`)

저장은 스트로크 종료 시 append-only 쓰기. 자동저장 로직 따로 안 만듦 — 항상 저장된 상태다.

Room DB(`index.db`)는 노트 메타 + FTS5 전문검색만. 유실돼도 파일에서 재생성 가능한 캐시로 취급.

접근은 SAF(Storage Access Framework)로 사용자가 고른 폴더에. → 구글드라이브/드롭박스 앱이 알아서 동기화해준다. **동기화를 직접 만들지 않는 이유.**

## 4. 제스처 — 규칙표

가장 중요한 건 화려한 제스처가 아니라 **의도 판정이 절대 틀리지 않는 것**이다.

| 입력 | 동작 |
|---|---|
| 펜 (`TOOL_TYPE_STYLUS`) | 그리기. **항상.** |
| 손가락 1개 | 팬. 절대 그리지 않음 (팜리젝션의 본질) |
| 손가락 2개 | 핀치 줌 + 팬 |
| 펜 배럴 버튼 누른 채 | 지우개 |
| 펜 뒤집기 (`TOOL_TYPE_ERASER`) | 지우개 |
| 손가락 2개 탭 | 실행취소 |
| 손가락 3개 탭 | 다시실행 |
| 펜 길게 누르기 | 올가미(lasso) 선택 |
| 그린 직후 멈춤 유지 | 도형 인식 (원/사각/직선 스냅) |

`getToolType()` 기반 분기 하나로 팜리젝션의 90%가 해결된다. 별도 알고리즘 불필요.

## 5. AI 사이드바

요청대로 "앱 안에서 AI 웹사이트가 열리는 사이드 패널".

- Compose 분할 레이아웃 + `AndroidView { WebView }`. 드래그로 폭 조절.
- 로그인 세션은 WebView 쿠키에 유지 → 한 번 로그인하면 계속 감.
- 노트 → AI 전달: 올가미로 선택 → ML Kit로 텍스트 변환 → WebView 입력창에 주입(또는 클립보드).
- AI → 노트: WebView에서 복사한 텍스트를 캔버스에 텍스트 블록으로 붙여넣기.

**리스크 (미리 알아둘 것):** 일부 AI 사이트는 WebView 접속을 차단하거나 UA로 막는다.
P3 시작 전에 **claude.ai / chatgpt.com이 WebView에서 실제로 로그인·동작하는지 30분짜리 스파이크로 먼저 확인**한다.
막히면 대안은 (a) Custom Tabs로 띄우기(분할화면은 OS 멀티윈도로), (b) Claude API 직접 호출.

## 6. 문서 임포트 — 난이도 순

1. **txt / md** — 그냥 읽음. 반나절.
2. **PDF** — `androidx.pdf`로 페이지 렌더 → 배경 이미지, 그 위에 잉크 레이어. 주석 PDF 내보내기는 나중.
3. **docx** — Apache POI는 안드로이드에서 무겁다. 텍스트만 필요하면 XML 직접 파싱(docx는 zip+xml)이 훨씬 가볍다.
4. **pptx** — 슬라이드를 이미지로 렌더링해야 해서 가장 비쌈. **정말 필요한지 P4에서 재검토.**

## 7. 단계

| 단계 | 내용 | 판정 기준 |
|---|---|---|
| **P0 스파이크** | 앱 껍데기 + ink API로 선 하나 긋기 | **실기기 지연 실측.** 목표 미달이면 여기서 설계 재검토. 이 단계 전에 다른 코드 한 줄도 쓰지 않는다 |
| P1 | 노트/페이지, 저장 포맷, 실행취소, 펜/형광펜/지우개/올가미 | 실사용으로 하루치 필기 가능 |
| P2 | 무한 캔버스, 팬/줌, 뷰포트 컬링, 제스처 전체 | 스트로크 1만개에서 60fps |
| P3 | WebView AI 사이드바 (스파이크 먼저) | — |
| P4 | 임포트 (txt/md → pdf → docx) | — |
| P5 | ML Kit OCR + FTS5 검색 | — |
| P6 | UI/디자인 확정 | 요청대로 기능 후에 |

각 단계는 이전 단계가 실기기에서 동작할 때만 시작한다.

## 8. 미확정

- ~~대상 기종~~ → **갤럭시탭 (S Pen) 확정.**
- ~~최소 API 레벨~~ → minSdk 29 확정. (ink 자체는 23이지만 프론트버퍼 경로가 Q부터 안정적)
